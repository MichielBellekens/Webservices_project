<% include Base_elements/Header %>
<h1><%= title %></h1>
<ul class="nav nav-tabs">
    <% for (var i =0; i < tab.length;i++){
    if(current_category == tab[i].link)
    {%>
    <li role="presentation" class="active"><a href=<%=tab[i].link%>><%=tab[i].text %></a></li>
    <%}
    else
    {%>
    <li role="presentation"><a href=<%=tab[i].link%>><%=tab[i].text %></a></li>
    <%}}%>
    <%if(cur_user == null){%>
    <li>
        <form class="form-inline" action="/login" method="post">
            <div class="form-group">
                <label class="sr-only" for="exampleInputEmail3">Email address</label>
                <input name="email" class="form-control" id="exampleInputEmail3" placeholder="Email">
            </div>
            <div class="form-group">
                <label class="sr-only" for="exampleInputPassword3">Password</label>
                <input name="password" class="form-control" type="password" id="exampleInputPassword3" placeholder="Password">
            </div>
            <button type="submit" class="btn btn-default">Sign in</button>
        </form>
    </li>
    <li>
        <a href="create_account">Create account</a>
    </li>
    <%}else{%>
    <li>
        <a class="btn btn-danger" href="/logout">logout</a>
    </li>
    <%}%>
</ul>
    <div class="row">
        <div class="panel panel-primary col-md-offset-3 col-md-6">
            <div class="panel-heading">Home page</div>
            <div class="panel-body">
                <% include ../static_content/Index_content.html%>
            </div>
        </div>
    </div>
<% include Base_elements/Footer %>